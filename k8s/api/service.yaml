apiVersion: v1
kind: Service
metadata:
  name: hospital-api-svc
  namespace: hospital
  labels:
    app: hospital-api
spec:
  type: ClusterIP
  selector:
    app: hospital-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP