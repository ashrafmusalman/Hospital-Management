apiVersion: apps/v1
kind: Deployment
metadata:
  name: hospital-admin
  namespace: hospital
  labels:
    app: hospital-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hospital-admin
  template:
    metadata:
      labels:
        app: hospital-admin
    spec:
      containers:
        - name: hospital-admin
          image: ashrafkhan0277/hospital-admin:91
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: API_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: hospital-config
                  key: API_BASE_URL
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"