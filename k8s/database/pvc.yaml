apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hospital-db-pvc
  namespace: hospital
  labels:
    app: hospital-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi